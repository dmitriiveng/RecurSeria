import std;
import recurseria.tests.yaml_target;
import recurseria.tests.binary_target;

int main() {
    recurseria::tests::yaml_target::test_yaml();
    recurseria::tests::binary_target::test_binary();
}
